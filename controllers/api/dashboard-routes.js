const router = router('express').Router();
const {Models} = require();

// routes for api/dashboard

// get userinfo
router.get('/', (req, res) => {

})

// get all liked pets
router.get('/', (req, res) => {

})

module.exports = router;