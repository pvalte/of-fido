const router = router('express').Router();
const {Models} = require();

// routes for api/pets
// get all pets
router.get('/', (req, res) => {

});

// get pets based on their category
router.get('/category/:id', (req, res) => {

});

// get a single pet
router.get('/:id', (req, res) => {

});

module.exports = router;